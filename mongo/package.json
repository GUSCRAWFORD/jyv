{
  "name": "@jyv/mongo",
  "version": "1.0.4",
  "description": "Out-of-the-box Generic Repository via MongoClient for NodeJS",
  "main": "./dist/bin/main.js",
  "types": "./dist/main.d.ts",
  "repository": "https://github.com/GUSCRAWFORD/jyv",
  "author": "Gus",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build:deps": "echo \"⚙️  Building dependencies\" cd ../core && yarn install && yarn build && cd ../mongo",
    "clean": "echo \"🗑  Cleaning build...\" && rimraf dist",
    "build": "echo \"🛠  Building...\" && tsc -diagnostics",
    "build:release": "yarn clean && yarn build && fpm transform . && cp README.md CHANGELOG.md dist/.",
    "docs": "typedoc --out docs src"
  },
  "peerDependencies": {
    "@jyv/core": "1.0.2",
    "@types/node": "*",
    "mongodb": "^3.1.4",
    "typescript": "^2.7.2"
  },
  "devDependencies": {
    "@guscrawford.com/fpm": "^0.0.10",
    "@jyv/core": "1.0.1",
    "@types/mongodb": "^3.1.18",
    "@types/node": "*",
    "mongodb": "^3.1.4",
    "rimraf": "^2.6.3",
    "typedoc": "^0.14.1",
    "typescript": "^2.7.2"
  },
  "@fpm:transform": {
    "scripts": "@fpm:remove",
    "devDependencies": "@fpm:remove",
    "main": {
      "@fpm:replace": {
        "dist/": ""
      }
    },
    "types": {
      "@fpm:replace": {
        "dist/": ""
      }
    }
  }
}
